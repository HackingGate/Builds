name: OpenWrt Image Builder Build

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build with Latest OpenWrt imagebuilder
        run: ./r7800-latest-openwrt-imagebuilder-build.sh
