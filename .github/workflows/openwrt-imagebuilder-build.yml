name: OpenWrt Image Builder Build

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Download Latest OpenWrt imagebuilder
        run: ./clone-latest-openwrt-tag.sh
